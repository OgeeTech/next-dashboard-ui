export default function DashboardLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return <div className="h-screen flex">
    {/* left */}
    <div className="left">
        l
    </div>

    {/* right */}
    <div className="right">
        r
    </div>
     </div>
}